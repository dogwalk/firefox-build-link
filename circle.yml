machine:
  node:
    version: 5
test:
  override:
    - npm run lint
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - npm run build:addon && ./bin/release-circleci.js --username dogwalk --repository firefox-build-link-plain
general:
  branches:
    ignore:
      - /.*/
