machine:
  node:
    version: 5
test:
  override:
    - npm run lint
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - npm run build:addon:signed && ./bin/release-circleci.js
general:
  branches:
    ignore:
      - /.*/
