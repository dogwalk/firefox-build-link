machine:
  node:
    version: 5
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - npm run build:addon && ./bin/release-circleci.js
general:
  branches:
    ignore:
      - /.*/
